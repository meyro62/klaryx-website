# ============================================================
# VERALTET – ersetzt durch klrx-settlement.yml
# ------------------------------------------------------------
# Dieser Workflow war ein Mock (sendete nie echtes KLRX).
# Die echte, idempotente On-Chain-Auszahlung übernimmt jetzt
# .github/workflows/klrx-settlement.yml
#
# Kein Zeitplan mehr -> läuft nicht mehr automatisch.
# Du kannst diese Datei in GitHub gefahrlos löschen.
# ============================================================
name: DEPRECATED - Weekly Referral Payout (Mock)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Hinweis
        run: echo "Veraltet. Nutze klrx-settlement.yml. Diese Datei kann geloescht werden."
